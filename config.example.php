<?php
// Скопировать в config.php на сервере и заполнить.
// config.php в .gitignore — в репозиторий он попасть не должен.
//
// Значения можно держать и в переменных окружения — getenv() имеет приоритет.

return [
    // Ключ Anthropic. Живёт только здесь, на фронтенд не попадает.
    'anthropic_key' => getenv('ANTHROPIC_API_KEY') ?: 'sk-ant-...',

    // Модель для распознавания чеков.
    'model' => 'claude-opus-5',

    // ID проекта Firebase — проверяется как aud в ID-токене.
    'firebase_project_id' => getenv('FIREBASE_PROJECT_ID') ?: 'mony-y-hac-b-kormaney',

    // Кто имеет право дёргать прокси.
    // Пустой массив = любой, кто вошёл в приложение через Google.
    // Расход при этом держит только rate_limit_per_hour.
    'allowed_emails' => [],

    // Откуда принимаем запросы. Пустой массив = запретить всё.
    'allowed_origins' => [
        'https://mybudget.sitemarket.co.il',
        'http://localhost:8080',
    ],

    // Лимит AI-запросов на пользователя в час.
    'rate_limit_per_hour' => 60,

    // Кому разрешено рассылать письма о новом. Проверяется на сервере:
    // спрятанной кнопки в админ-панели для этого мало.
    'admin_emails' => ['sashad2003@gmail.com'],

    // Чем отправлять письма: 'smtp' или 'ahasend'.
    //
    // smtp — обычная почта хостинга или Gmail, стороннего сервиса не нужно.
    // Для десятков писем этого достаточно: Hostinger пропускает 500 писем в
    // час, Gmail около 500 адресов в сутки.
    //
    // ahasend — почтовый сервис по HTTP API v2. Нужен, когда важны статистика
    // доставки, обработка отписок и жалоб или объёмы за тысячи писем.
    'mail_transport' => 'smtp',

    // От кого приходит письмо.
    //
    // Адрес заводится на своём домене, в почте хостинга: письмо о приложении,
    // пришедшее с чужого gmail-адреса, почтовики метят как подделку, потому
    // что домен отправителя не совпадает с доменом сайта. С ящиком на домене
    // подписи SPF и DKIM у Hostinger уже настроены.
    'mail_from_email' => 'mybudget@sitemarket.co.il',
    'mail_from_name'  => 'Семейный бюджет',

    // Куда придёт ответ, если человек всё-таки нажмёт «ответить». В письме
    // отвечать не предлагается — там кнопка WhatsApp, — но ящик приложения
    // никто не читает, и ответ не должен пропасть. Пусто — ответы уйдут на
    // mail_from_email.
    'mail_reply_to' => 'sashad2003@gmail.com',

    // SMTP. Порт 465 — шифрование сразу, 587 — STARTTLS после приветствия.
    //   Hostinger: smtp.hostinger.com, логин — сам почтовый адрес
    //   Gmail:     smtp.gmail.com, пароль приложения (обычный не подойдёт),
    //              и mail_from_email тогда обязан быть тем же gmail-адресом:
    //              переписать отправителя Gmail не даёт
    'smtp_host' => 'smtp.hostinger.com',
    'smtp_port' => 465,
    'smtp_user' => getenv('SMTP_USER') ?: 'mybudget@sitemarket.co.il',
    'smtp_pass' => getenv('SMTP_PASS') ?: '',

    // AhaSend — если выбран транспорт 'ahasend'.
    // Ключ вида aha-sk-..., id аккаунта из панели. Домен отправителя должен
    // быть подтверждён там же (SPF и DKIM), иначе письма уедут в спам.
    'ahasend_key'        => getenv('AHASEND_API_KEY') ?: '',
    'ahasend_account_id' => getenv('AHASEND_ACCOUNT_ID') ?: '',
];
